<view class="container">
    <view class="topBar">
        <image bindtap="showModal" class="leftAdd" src="../../images/BTN.png"></image>
        <text>教室</text>
        <image bindtap="deleteKeCheng" class="rightDel" src="../../images/BTN.png"></image>
    </view>
</view>

<view class="container-cn">
    <view class="item">
        <view class="stuInfo">教室1</view>
        <label class="checkBox" catchtap='checkedTap'>
            <checkbox checked="{{checked}}" color='#fff' />
        </label>
    </view>

    <view class="item">
        <view class="stuInfo">教室2</view>
        <label class="checkBox" catchtap='checkedTap'>
            <checkbox checked="{{checked}}" color='#fff' />
        </label>
    </view>

    <view class="item">
        <view class="stuInfo">教室3</view>
        <label class="checkBox" catchtap='checkedTap'>
            <checkbox checked="{{checked}}" color='#fff' />
        </label>
    </view>

    <view class="item">
        <view class="stuInfo">教室4</view>
        <label class="checkBox" catchtap='checkedTap'>
            <checkbox checked="{{checked}}" color='#fff' />
        </label>
    </view>

    <view class="item">
        <view class="stuInfo">教室5</view>
        <label class="checkBox" catchtap='checkedTap'>
            <checkbox checked="{{checked}}" color='#fff' />
        </label>
    </view>

    <view class="item">
        <view class="stuInfo">教室6</view>
        <label class="checkBox" catchtap='checkedTap'>
            <checkbox checked="{{checked}}" color='#fff' />
        </label>
    </view>

    <view class="item">
        <view class="stuInfo">教室7</view>
        <label class="checkBox" catchtap='checkedTap'>
            <checkbox checked="{{checked}}" color='#fff' />
        </label>
    </view>
</view>

<view class="commodity_screen" bindtap="hideModal" wx:if="{{showModalStatus}}"></view>
<view animation="{{animationData}}" class="commodity_attr_box" wx:if="{{showModalStatus}}">
    <view class="title">
        <text>录入教室</text>
    </view>
    <view class="linkItem">
        <view class="leftImg">
            <image src="../../images/classRoomBar.png" mode="widthFix"></image>
        </view>
        <view class="txt">
            <input type="text" placeholder="请录入课程"></input>
        </view>
    </view>
    <view class="rightBtn" bindtap="hideModal" wx:if="{{showModalStatus}}">
        <text class="rightTxt">确定</text>
    </view>
</view>